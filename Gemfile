# A sample Gemfile
source 'https://rubygems.org'

# Declare a ruby version but don't throw error if actual version is newer
ruby '2.2.0' unless RUBY_VERSION != '2.2.0' && RUBY_VERSION.to_f >= 2.2

gem 'dotenv'
gem 'ruby-progressbar'
