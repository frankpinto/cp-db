# A sample Gemfile
source 'https://rubygems.org'

# Declare a ruby version but don't throw error if actual version is newer
ruby '3.3.1' unless RUBY_VERSION != '3.3.1' && RUBY_VERSION.to_f >= 3.3

gem 'dotenv'
gem 'ruby-progressbar'
